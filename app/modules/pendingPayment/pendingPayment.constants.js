const PendingPaymentSearchAbleFields = ["user_id", "branch"];
const PendingPaymentFilterAbleFileds = ["searchTerm", "user_id", "branch"]; // Now used for exact filter

module.exports = {
  PendingPaymentFilterAbleFileds,
  PendingPaymentSearchAbleFields,
};
